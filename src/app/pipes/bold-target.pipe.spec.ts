import { BoldTargetPipe } from './bold-target.pipe';

describe('BoldTargetPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldTargetPipe();
    expect(pipe).toBeTruthy();
  });
});
